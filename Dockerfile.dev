FROM node:20-alpine

WORKDIR /usr/src/app

# Copy dependency manifests
COPY package*.json ./

# Install all dependencies (dev + prod)
RUN npm install

# Copy everything else
COPY . .

# Expose dev port
EXPOSE 3000

# Default command runs ts-node-dev for hot reload
CMD ["npm", "run", "dev"]
